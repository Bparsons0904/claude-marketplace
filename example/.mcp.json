{
  "mcpServers": {
    "example-server": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://example-mcp-server.com/mcp"],
      "description": "Example MCP server configuration. Replace with your actual MCP server details."
    }
  }
}
